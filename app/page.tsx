export default function HomePage() {
  return (
    <main className="mx-auto flex min-h-screen w-full max-w-6xl items-center justify-center px-6 py-12">
      <section className="card max-w-2xl space-y-4 p-8 text-center">
        <h1 className="text-4xl font-semibold tracking-tight">AutoFlow</h1>
        <p className="text-base text-[var(--muted)]">
          Scaffold complete. Phase 0 provides typed contracts, build tooling, and backend bootstrap.
        </p>
      </section>
    </main>
  );
}
